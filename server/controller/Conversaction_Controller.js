export const newConversaction = async (req, res) => {
  try {
    const senderId = req.body.senderId;
    const receiverId = req.body.receiverId;
    // const exist=
  } catch (err) {
    console.log(err);
  }
};
